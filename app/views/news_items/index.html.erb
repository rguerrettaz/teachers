<%#= @search %>
<h1>Popular Items</h1>
<% @news_items.each do |item| %>
  <article class= <%= item.type %> >
    <%= item.type %>
    
    <%= render "#{item.type}", :item => item %>

  </article>
<% end %>        
